function renderFriendsList(){const box=document.getElementById('friendsList');if(!box)return;const list=friendSnapshots(),myStats=snapshotStats(loadJson(HISTORY_KEY,[]));if(!list.length){box.innerHTML='<div class="emptySocial">Aucune amie ajoutée pour le moment.</div>';return}box.innerHTML=list.map(friend=>{const mode=friend.stats.mode==='duo'?'Duo':friend.stats.mode==='solo'?'Solo':'—',difference=(myStats.total||0)-(friend.stats.total||0),comparison=difference===0?'Vous avez le même nombre de défis.':difference>0?`Tu as ${difference} défi${difference>1?'s':''} de plus.`:`${friend.name} a ${Math.abs(difference)} défi${Math.abs(difference)>1?'s':''} de plus.`;return `<article class="friendItem"><div class="friendHead"><div class="friendIdentity"><div class="friendAvatar">${escapeText(friend.name.charAt(0).toUpperCase())}</div><div class="friendName"><b>${escapeText(friend.name)}</b><span>Mis à jour le ${formatDate(friend.generatedAt)}</span></div></div><div class="friendActions"><button class="removeFriend" onclick="removeFriend('${escapeText(friend.id)}')">Retirer</button></div></div><div class="friendStats"><div class="friendStat"><b>${friend.stats.total||0}</b><span>Défis</span></div><div class="friendStat"><b>${friend.stats.minutes||0}</b><span>Minutes</span></div><div class="friendStat"><b>${mode}</b><span>Mode</span></div></div><div class="comparison">${escapeText(comparison)}</div></article>`}).join('')}
function buildFeedItems(){const own=loadJson(HISTORY_KEY,[]).map(item=>({...item,owner:user?.name||'Moi',ownerId:'self'})),friends=friendSnapshots().flatMap(friend=>(friend.recent||[]).map(item=>({...item,owner:friend.name,ownerId:friend.id})));return own.concat(friends).sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,40)}
function renderFeed(){const box=document.getElementById('feedList');if(!box)return;const items=buildFeedItems();if(!items.length){box.innerHTML='<div class="emptySocial">Le fil se remplira après ton premier défi ou après l’ajout d’une amie.</div>';return}box.innerHTML=items.map(item=>`<article class="feedItem"><div class="feedAvatar">${escapeText((item.owner||'?').charAt(0).toUpperCase())}</div><div class="feedContent"><b>${escapeText(item.owner||'Participante')}<span class="onlineBadge">${item.ownerId==='self'?'Moi':'Amie'}</span></b><p>${escapeText(item.emoji||'✨')} ${escapeText(item.title||'Défi terminé')}</p><span>${formatDate(item.date)} • ${item.mode==='duo'?'Duo':'Solo'} • ${LEVEL_LABELS[item.level]||item.level||'Niveau libre'}${item.time?' • '+escapeText(item.time):''}</span></div></article>`).join('')}
function renderSocialExtras(){renderFriendsList();renderFeed();refreshMyFriendCode()}
function openFeed(){renderFeed();show('feed')}
