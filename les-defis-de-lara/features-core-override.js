draw=function(){const list=window.CHALLENGES?.[mode]?.[level]||[];if(!list.length)return alert('Aucun défi disponible pour ce niveau.');let next=list[Math.floor(Math.random()*list.length)];if(current&&list.length>1){while(next.title===current.title)next=list[Math.floor(Math.random()*list.length)]}current=next;$('ce').textContent=current.emoji;$('ct').textContent=current.title;$('cd').textContent=current.description;$('cc').textContent=current.category;$('tip').textContent='Adapte toujours les charges, le rythme et l’amplitude à ton niveau.';updateFavoriteButton()};
