function displayProfilePhoto(){const avatar=document.getElementById('avatar');if(!avatar)return;const photo=localStorage.getItem(profilePhotoKey());if(photo){avatar.style.backgroundImage=`url("${photo}")`;avatar.classList.add('hasPhoto');avatar.textContent=''}else{avatar.style.backgroundImage='';avatar.classList.remove('hasPhoto');avatar.textContent=(user?.name||'L').charAt(0).toUpperCase()}}
async function resizeProfilePhoto(file){const data=await new Promise((resolve,reject)=>{const reader=new FileReader;reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file)});const image=await new Promise((resolve,reject)=>{const picture=new Image;picture.onload=()=>resolve(picture);picture.onerror=reject;picture.src=data});const canvas=document.createElement('canvas');canvas.width=256;canvas.height=256;const context=canvas.getContext('2d'),size=Math.min(image.width,image.height),sourceX=(image.width-size)/2,sourceY=(image.height-size)/2;context.drawImage(image,sourceX,sourceY,size,size,0,0,256,256);return canvas.toDataURL('image/jpeg',.82)}
